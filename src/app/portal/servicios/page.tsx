import { portalContentSection } from '@/app/_shared/portal-content-section';

export const dynamic = 'force-dynamic';

/** Own services management (task 6.3). */
export default portalContentSection('services');
