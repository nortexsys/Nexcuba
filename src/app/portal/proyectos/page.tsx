import { portalContentSection } from '@/app/_shared/portal-content-section';

export const dynamic = 'force-dynamic';

/** Own projects management (task 6.3). */
export default portalContentSection('projects');
