// Test stub for the `server-only` package: its browser build throws on import,
// which is exactly the production behavior we want to keep — but irrelevant
// (and disruptive) inside the jsdom test environment.
export {};
